import React from 'react';
import { Link } from 'react-router-dom';
import { Card } from 'react-bootstrap';

const Product = ({ product }) => {
	return <Card className="my-1 p-6"></Card>;
};

export default Product;
